declare var React: any;

declare global {
  interface Window {
    // Pose?: any; //mediapipe pose
    // Camera?: any; //mediapipe camera
    user?: any;
  }
}

interface Navigator {
  deviceMemory?: any;
}
